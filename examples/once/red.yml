once/workdir: work

# -> compute  (the VPS; provider is chosen by the "once/provider" advice, not here)
once/host: {name: vps-1, region: fra1, size: s-1vcpu-1gb}

# -> compute  (public keys seeded into the VPS authorized_keys)
once/ssh:
  compute: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAExampleComputeKey once-compute
  deploy: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAExampleDeployKey github-actions-deploy

# -> smtp + dns + ansible  (the sites Basecamp ONCE runs on the box)
once/website:
  - {name: campfire, image: "37signals/campfire:latest", hostname: chat.example.com}
  - {name: writebook, image: "37signals/writebook:latest", hostname: books.example.com}
